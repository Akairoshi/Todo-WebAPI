info:
  name: Delete note
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:5254/Note/{{noteId}}
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
