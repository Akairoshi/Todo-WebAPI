info:
  name: Get one note
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5254/Note/{{noteId}}
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
