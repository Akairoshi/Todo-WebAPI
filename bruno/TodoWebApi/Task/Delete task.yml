info:
  name: Delete task
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:5254/Task/{{noteId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
